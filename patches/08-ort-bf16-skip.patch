diff --git a/onnxruntime/core/providers/migraphx/migraphx_execution_provider.cc b/onnxruntime/core/providers/migraphx/migraphx_execution_provider.cc
--- a/onnxruntime/core/providers/migraphx/migraphx_execution_provider.cc
+++ b/onnxruntime/core/providers/migraphx/migraphx_execution_provider.cc
@@ -1250,7 +1250,8 @@ void calibrate_and_quantize(migraphx::program& prog,
 #if HIP_VERSION_MAJOR > 6 || (HIP_VERSION_MAJOR == 6 && HIP_VERSION_MINOR >= 4 && HIP_VERSION_PATCH >= 2)
   if (bf16_enable) {
     LOGS_DEFAULT(WARNING) << "Quantizing input program to bf16";
-    migraphx::quantize_bf16(prog);
+    // quantize_bf16 not available in MIGraphX ROCm 6.4
+    LOGS_DEFAULT(WARNING) << "BF16 quantization not available in this MIGraphX build, skipping";
     LOGS_DEFAULT(WARNING) << "Quantizing bf16: Complete";
   }
 #endif
